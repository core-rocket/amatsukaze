name: build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Install PlatformIO
      run: |
        pip --version
        pip install --user -U platformio
        platformio --version
    - name: build
      run: |
        platformio run
