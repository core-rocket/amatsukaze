name: build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Install PlatformIO
      run: |
        pip --version
        pip install platformio
        platformio --version
    - name: build
      uses: actions/checkout@v1
      run: |
        platformio run
