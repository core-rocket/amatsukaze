name: build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Install PlatformIO
      run: |
        pip --version
        pip install wheel
        pip install -U platformio
        pip platformio --version
    - name: build
      run: |
        pip platformio run
